package com.example.app_definida.ui.main

import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {
    // Este ViewModel se puede usar para compartir datos entre las pantallas principales
    // Por ahora, lo dejamos vacío hasta que se necesite.
}
